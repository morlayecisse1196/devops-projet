# ==============================================
# Configuration Déploiement - Gestion Événement Magal
# ==============================================

# ==============================================
# DATABASE (PostgreSQL)
# ==============================================
POSTGRES_DB=magal_db
POSTGRES_USER=magal_user
POSTGRES_PASSWORD=changeme_secure_password_here
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ==============================================
# DJANGO BACKEND
# ==============================================
DJANGO_SECRET_KEY=django-insecure-change-this-in-production-use-minimum-50-chars
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
DJANGO_CORS_ORIGINS=http://localhost,http://localhost:3000,https://your-domain.com

# Database URL for Django (auto-constructed from above)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ==============================================
# DOCKER IMAGES (from Docker Hub)
# ==============================================
BACKEND_IMAGE=your-dockerhub-username/backend-magal:latest
FRONTEND_IMAGE=your-dockerhub-username/frontend-magal:latest
POSTGRES_IMAGE=postgres:15-alpine

# ==============================================
# FRONTEND (React/Vite)
# ==============================================
# API URL that frontend will use to call backend
VITE_API_URL=http://localhost:8000/api

# Production example:
# VITE_API_URL=https://api.your-domain.com/api

# ==============================================
# PORTS (Host mapping)
# ==============================================
BACKEND_PORT=8000
FRONTEND_PORT=80
# FRONTEND_PORT=3000  # Pour développement

# NOTE: PostgreSQL port 5432 n'est pas exposé par défaut
# pour des raisons de sécurité. Décommenter si nécessaire.
# POSTGRES_EXTERNAL_PORT=5432

# ==============================================
# VOLUMES
# ==============================================
# Les volumes sont définis dans docker-compose.yml
# Pas besoin de les configurer ici

# ==============================================
# ENVIRONMENT
# ==============================================
ENVIRONMENT=production
# ENVIRONMENT=development
# ENVIRONMENT=staging

# ==============================================
# BACKUP (Optionnel)
# ==============================================
BACKUP_DIR=./backups
BACKUP_RETENTION_DAYS=7

# ==============================================
# MONITORING (Optionnel)
# ==============================================
# ENABLE_MONITORING=true
# PROMETHEUS_PORT=9090
# GRAFANA_PORT=3000
